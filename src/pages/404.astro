---
import BaseLayout from "@layouts/BaseLayout.astro";

import { SITE_SUFFIX } from "../consts";
---

<BaseLayout title={"404 Not Found" + SITE_SUFFIX} description={"404 Not Found"}>
  <div>
    <div
      class="border-theme container mx-auto flex min-h-dvh items-center justify-center border-x p-24"
    >
      <div class="border-theme relative border-2 p-8">
        <div class="bg-pattern absolute inset-0 -z-10 h-full"></div>
        <div
          class="bg-theme border-theme flex flex-col gap-4 border p-4 text-center"
        >
          <h1 class="text-4xl font-semibold">404 Not Found</h1>
          <p class="text-muted">You're on a page that does not exist</p>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>
